spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud?autoReconnect=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
